apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: monitorable-application-monitor
spec:
  selector:
    matchLabels:
      app: user-app
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s